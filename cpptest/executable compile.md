Hey thanks checking out the assignment. To init the program just copy and paste this to console. Thanks!
pre-rec compiler is g++ and gcc and add path to env-path.
```
g++ ./src/main.cpp -o main -static -static-libstdc++
./main
```